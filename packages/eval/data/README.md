# Eval Data

Place the fixed held-out eval set here.

Recommended file:

- `eval-set.csv`

`eval-set.csv` is intended to be the tracked, license-safe fixed benchmark set for
this repo.

Do not commit raw Kakao Brain Jejueo corpus files to the public repo if your
chosen licensing posture requires keeping raw data out of version control.
